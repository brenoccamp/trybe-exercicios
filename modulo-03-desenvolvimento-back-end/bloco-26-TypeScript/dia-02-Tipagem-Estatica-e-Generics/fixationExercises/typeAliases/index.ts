type Bird = {
  specie: string,
  wings: number,
  biped: true,
};

type Sum = (n1: number, n2: number) => number;

type Address = {
  state: string,
  city: string,
  street: string,
  district: string,
  houseNumber: number,
};

type StatesOfMatter = 'liquid' | 'solid' | 'gaseous';

type personId = string | number;

type OperationSystem = 'linux' | 'mac' | 'windows';

type AlphabetVowels = 'a' | 'e' | 'i' | 'o' | 'u';
type Bird = {
  specie: string,
  wings: number,
  biped: true,
};

type Sum = (n1: number, n2: number) => number;

type Address = {
  city: string,
  street: string,
  district: string,
  houseNumber: number,
}